package textdecorators;

/**
 * Abstract Text Decorator is an abstract class which has only method processInputDetails() with no
 * arguments and all decorators are  derived types of AbstractTextDecorator
 *
 * @author Krupa Sawant
 */
public abstract class AbstractTextDecorator {
	public abstract void processInputDetails();
}